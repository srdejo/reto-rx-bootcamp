spring:
  r2dbc:
    url: r2dbc:h2:mem:///bootcamp_test;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password: ""
  sql:
    init:
      mode: always
webclient:
  capacity: ${WEBCLIENT_CAPACITY:http://localhost:8083/api/v1/bootcamp-capacities}

allowed-origins: ${ALLOWED_ORIGIN:http://localhost:4200}

appDescription: "Pragma Reactive Bootcamp"
appVersion: "1.0.0"

jwt:
  secret: ${JWT_SECRET:change-this-secret-change-this-secret-32bytes}
